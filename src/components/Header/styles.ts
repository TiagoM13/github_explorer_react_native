import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    backgroundColor: '#000',
    alignItems: 'center',
    paddingVertical: 16,
    marginBottom: 16,
  },
  text: {
    color: '#FFF',
    fontSize: 18,
    fontFamily: 'Inter_900Black',
  },
});
